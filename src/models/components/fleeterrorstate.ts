/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type FleetErrorState = {
  lastErrorTimestamp: Date | null;
  lastErrorMessage: string | null;
  lastErrorLogId: string | null;
  inError: boolean;
};

/** @internal */
export const FleetErrorState$inboundSchema: z.ZodType<
  FleetErrorState,
  z.ZodTypeDef,
  unknown
> = z.object({
  lastErrorTimestamp: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ),
  lastErrorMessage: z.nullable(z.string()),
  lastErrorLogId: z.nullable(z.string()),
  inError: z.boolean(),
});

export function fleetErrorStateFromJSON(
  jsonString: string,
): SafeParseResult<FleetErrorState, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FleetErrorState$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FleetErrorState' from JSON`,
  );
}
