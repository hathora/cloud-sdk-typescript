/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { tokensV1CreateOrgToken } from "../../funcs/tokensV1CreateOrgToken.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  orgId: z.string(),
  createOrgToken: components.CreateOrgToken$inboundSchema,
};

export const tool$tokensV1CreateOrgToken: ToolDefinition<typeof args> = {
  name: "tokens-v1_create-org-token",
  description: `CreateOrgToken

Create a new organization token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tokensV1CreateOrgToken(
      client,
      args.orgId,
      args.createOrgToken,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
