/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { organizationsV1GetOrgPendingInvites } from "../../funcs/organizationsV1GetOrgPendingInvites.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  orgId: z.string(),
};

export const tool$organizationsV1GetOrgPendingInvites: ToolDefinition<
  typeof args
> = {
  name: "organizations-v1_get-org-pending-invites",
  description: `GetOrgPendingInvites`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsV1GetOrgPendingInvites(
      client,
      args.orgId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
