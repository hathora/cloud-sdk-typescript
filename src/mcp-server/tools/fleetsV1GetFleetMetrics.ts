/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fleetsV1GetFleetMetrics } from "../../funcs/fleetsV1GetFleetMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFleetMetricsRequest$inboundSchema,
};

export const tool$fleetsV1GetFleetMetrics: ToolDefinition<typeof args> = {
  name: "fleets-v1-get-fleet-metrics",
  description: `GetFleetMetrics

Gets metrics for a [fleet](https://hathora.dev/docs/concepts/hathora-entities#fleet) in a region.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fleetsV1GetFleetMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
