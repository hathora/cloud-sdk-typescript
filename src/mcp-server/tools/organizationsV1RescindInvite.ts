/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { organizationsV1RescindInvite } from "../../funcs/organizationsV1RescindInvite.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  orgId: z.string(),
  rescindUserInvite: components.RescindUserInvite$inboundSchema,
};

export const tool$organizationsV1RescindInvite: ToolDefinition<typeof args> = {
  name: "organizations-v1_rescind-invite",
  description: `RescindInvite`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsV1RescindInvite(
      client,
      args.orgId,
      args.rescindUserInvite,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
