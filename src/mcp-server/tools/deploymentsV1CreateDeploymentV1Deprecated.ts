/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { deploymentsV1CreateDeploymentV1Deprecated } from "../../funcs/deploymentsV1CreateDeploymentV1Deprecated.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  buildId: z.number().int(),
  deploymentConfig: components.DeploymentConfig$inboundSchema,
  appId: z.string().optional(),
};

export const tool$deploymentsV1CreateDeploymentV1Deprecated: ToolDefinition<
  typeof args
> = {
  name: "deployments-v1-create-deployment-v1-deprecated",
  description: `CreateDeploymentV1Deprecated

Create a new [deployment](https://hathora.dev/docs/concepts/hathora-entities#deployment). Creating a new deployment means all new rooms created will use the latest deployment configuration, but existing games in progress will not be affected.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsV1CreateDeploymentV1Deprecated(
      client,
      args.buildId,
      args.deploymentConfig,
      args.appId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
