/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { billingV1InitStripeCustomerPortalUrl } from "../../funcs/billingV1InitStripeCustomerPortalUrl.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  customerPortalUrl: components.CustomerPortalUrl$inboundSchema,
  orgId: z.string().optional(),
};

export const tool$billingV1InitStripeCustomerPortalUrl: ToolDefinition<
  typeof args
> = {
  name: "billing-v1_init-stripe-customer-portal-url",
  description: `InitStripeCustomerPortalUrl`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await billingV1InitStripeCustomerPortalUrl(
      client,
      args.customerPortalUrl,
      args.orgId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
