/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { roomsV2UpdateRoomConfig } from "../../funcs/roomsV2UpdateRoomConfig.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  roomId: z.string(),
  updateRoomConfigParams: components.UpdateRoomConfigParams$inboundSchema,
  appId: z.string().optional(),
};

export const tool$roomsV2UpdateRoomConfig: ToolDefinition<typeof args> = {
  name: "rooms-v2_update-room-config",
  description: `UpdateRoomConfig`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await roomsV2UpdateRoomConfig(
      client,
      args.roomId,
      args.updateRoomConfigParams,
      args.appId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
