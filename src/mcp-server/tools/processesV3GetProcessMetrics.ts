/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { processesV3GetProcessMetrics } from "../../funcs/processesV3GetProcessMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetProcessMetricsRequest$inboundSchema,
};

export const tool$processesV3GetProcessMetrics: ToolDefinition<typeof args> = {
  name: "processes-v3-get-process-metrics",
  description: `GetProcessMetrics`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await processesV3GetProcessMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
