/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { processesV1GetProcessInfoDeprecated } from "../../funcs/processesV1GetProcessInfoDeprecated.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  processId: z.string(),
  appId: z.string().optional(),
};

export const tool$processesV1GetProcessInfoDeprecated: ToolDefinition<
  typeof args
> = {
  name: "processes-v1_get-process-info-deprecated",
  description: `GetProcessInfoDeprecated

Get details for a [process](https://hathora.dev/docs/concepts/hathora-entities#process).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await processesV1GetProcessInfoDeprecated(
      client,
      args.processId,
      args.appId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
