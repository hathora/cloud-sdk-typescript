/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { billingV1GetInvoices } from "../../funcs/billingV1GetInvoices.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  orgId: z.string().optional(),
};

export const tool$billingV1GetInvoices: ToolDefinition<typeof args> = {
  name: "billing-v1-get-invoices",
  description: `GetInvoices`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await billingV1GetInvoices(
      client,
      args.orgId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
