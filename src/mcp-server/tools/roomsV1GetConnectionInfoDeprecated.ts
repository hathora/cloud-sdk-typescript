/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { roomsV1GetConnectionInfoDeprecated } from "../../funcs/roomsV1GetConnectionInfoDeprecated.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  roomId: z.string(),
  appId: z.string().optional(),
};

export const tool$roomsV1GetConnectionInfoDeprecated: ToolDefinition<
  typeof args
> = {
  name: "rooms-v1_get-connection-info-deprecated",
  description: `GetConnectionInfoDeprecated`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await roomsV1GetConnectionInfoDeprecated(
      client,
      args.roomId,
      args.appId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
